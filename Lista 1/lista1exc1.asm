addi $t0, $t0, 527	# insere valor inteiro 527 no registrador $t0
addi $t0, $t0, -372	# soma o valor 527 a -372 e armazena no mesmo registrador $t0
addi $t0, $t0, 225	# soma o valor 225 ao resultado anterior e armazena no mesmo registrador $t0
addi $t0, $t0, -794	# soma o valor -794 ao resultado anterior e armazena no mesmo registrador $t0